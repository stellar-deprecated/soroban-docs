---
sidebar_position: 2
title: Install Soroban CLI
---

Install the Soroban CLI that can execute WASM contracts in a local sandbox
environment.

```sh
cargo install --locked --git http://github.com/stellar/soroban-cli soroban-cli
```

:::caution
The soroban-cli is still in early development. Please report any issues
[here](https://github.com/stellar/soroban-cli).
:::

Run the `soroban-cli` command and you should see output like below.

```sh
soroban-cli
```

```
soroban-cli 0.0.1

USAGE:
    soroban-cli <SUBCOMMAND>

OPTIONS:
    -h, --help    Print help information

SUBCOMMANDS:
    invoke     Invoke a contract function in a WASM file
    inspect    Inspect a WASM file listing contract functions, meta, etc
    deploy     Writes a contractID and WASM contract directly to storage
    version    Print version information
```
