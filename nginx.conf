server {
    listen 80;
    listen [::]:80 default ipv6only=on;

    root /usr/share/nginx/html;
    index index.html;
    error_page 404 /404.html;

    server_name _; # all hostnames

    location / {
        try_files $uri $uri/ /index.html =404;
    }
    location /index.html {
        add_header 'Cache-Control' 'no-store' always;
    }
    location /assets/ {
        try_files $uri $uri/ =404;
    }
    location /404.html {
        add_header 'Cache-Control' 'no-store' always;
    }

    # Add custom headers to /.well-known
    # location /.well-known/ {
    #     add_header 'Access-Control-Allow-Origin' '*';
    #     add_header 'Content-Type' 'text/plain';
    # }

    # Redirect /bounties to github repo
    # rewrite ^/bounties$ https://github.com/tyvdh/stellar-quest-bounties redirect;

    # Redirect /join to Discord server
    # rewrite ^/join$ https://discord.gg/8FhvuKb redirect;

    # Redirect /not-a-cult-nft get rekt
    # rewrite ^/not-a-cult-nft$ 'https://www.youtube.com/watch?v=dQw4w9WgXcQ' redirect;
}